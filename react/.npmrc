# HeroUI (heroui) official pnpm setup:
# hoist @heroui/* packages to the root node_modules so the Tailwind v4
# "@source .../@heroui/theme/dist/**" scanning works with pnpm's isolated layout.
public-hoist-pattern[]=*@heroui/*