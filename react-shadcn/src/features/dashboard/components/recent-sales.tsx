/**
 * 最近销售占位。
 * 原组件包含硬编码的假销售记录，已按要求移除 Mock。
 * 真实数据需由后端提供接口后接入。
 */
export function RecentSales() {
  return (
    <div className='flex h-[300px] items-center justify-center text-sm text-muted-foreground'>
      暂无销售数据
    </div>
  )
}
